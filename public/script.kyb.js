const welcome = oneSdkInstance.component("form", {
    name: "WELCOME",
    mode: "business",
  });
  const consent = oneSdkInstance.component("form", {
    name: "CONSENT",
    mode: "business",
  });